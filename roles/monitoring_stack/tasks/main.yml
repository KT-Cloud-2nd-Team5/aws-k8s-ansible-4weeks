---
# 1. 기존 데이터 정리 (충돌 방지)
- name: 기존 설정 폴더 및 파일 초기화
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ monitoring_dir }}/provisioning"
    # [중요] 폴더로 잘못 생성되었을 경우를 대비해 삭제
    - "{{ monitoring_dir }}/prometheus.yml" 

# 2. 필요한 모든 '디렉토리'만 먼저 생성
- name: 모니터링 디렉토리 구조 생성
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ target_user }}"
    group: "{{ target_user }}"
    mode: '0755'
  loop:
    - "{{ monitoring_dir }}"
    - "{{ monitoring_dir }}/provisioning/datasources"
    - "{{ monitoring_dir }}/provisioning/dashboards"
    - "{{ monitoring_dir }}/provisioning/alerting"
    - "{{ monitoring_dir }}/dashboards"

# 3. 설정 파일 배포 (src: 원본템플릿, dest: 생성될파일명)
- name: 모니터링 설정 파일 배포
  template:
    src: "{{ item.src }}"
    dest: "{{ monitoring_dir }}/{{ item.dest }}"
    owner: "{{ target_user }}"
    group: "{{ target_user }}"
    mode: '0644'
    force: yes # 강제로 덮어쓰기 허용
  loop:
    - { src: 'docker-compose.yml.j2', dest: 'docker-compose.yml' }
    - { src: 'prometheus.yml.j2', dest: 'prometheus.yml' } # 파일로 생성됨
    - { src: 'datasource.yml.j2', dest: 'provisioning/datasources/datasource.yml' }
    - { src: 'dashboards-provider.yml.j2', dest: 'provisioning/dashboards/provider.yml' }
    - { src: 'alerting.yml.j2', dest: 'provisioning/alerting/alerting.yml' }
  notify: "reload monitoring"

# 4. 대시보드 다운로드 등 나머지 태스크...
- name: Grafana Labs에서 공식 대시보드 다운로드
  get_url:
    url: "https://grafana.com/api/dashboards/{{ item.id }}/revisions/{{ item.revision }}/download"
    dest: "{{ monitoring_dir }}/dashboards/dashboard-{{ item.id }}.json"
  loop: "{{ dashboard_ids }}"

# 5. 스택 실행 (모든 파일이 준비된 후 실행)
- name: 통합 모니터링 스택 실행
  community.docker.docker_compose_v2:
    project_src: "{{ monitoring_dir }}"
    state: present
    # [추가] 꼬인 컨테이너 정리를 위해 옵션 추가 추천
    remove_orphans: yes 
  become: yes
